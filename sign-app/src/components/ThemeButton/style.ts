import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        padding: 8,
        backgroundColor: "rgb(28, 28, 242)",
        borderRadius: 32,
        marginVertical: 16,
        boxShadow: "0px 2px 2px #00F"
    }
})